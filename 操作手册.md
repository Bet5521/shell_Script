# 操作手册 — Linux 运维一键脚本集

本手册提供每个脚本的详细操作命令、参数说明与示例。通用约定见后文「通用说明」，各脚本独立成章。

---

## 通用说明

- **提权**：需要 root 的操作，脚本会自动尝试 `sudo` / `pkexec` 提权；若两者都不可用会明确报错并以退出码 `1` 退出。也可自行 `sudo bash xxx.sh`。
- **非交互 / 静默**：大多数脚本支持 `-y` / `--yes` 跳过交互确认；`--dry-run` 可预演不落盘（支持的脚本）。
- **日志与调试**：`-v` / `--verbose` 输出调试信息；`NO_COLOR=1` 可关闭彩色输出（`set_mirror.sh` 支持）。
- **安全回滚**：改动系统前脚本会备份；失败时按脚本策略回滚。请勿在回滚完成前手动删除备份目录。

---

## 1. install_pkg.sh — 通用软件包安装工具

**版本**：2.0.0　**退出码**：`0` 成功 / `1` 常规错误 / `2` 参数错误 / `3` 权限不足 / `4` 不支持的包类型

### 1.1 功能概述

把本地软件包（归档 / 系统包 / 二进制 / AppImage / 安装脚本）安装到目标目录，并可按需创建桌面快捷方式、注册为系统服务。

支持的包类型（可 `--type` 手动指定，默认 `auto` 自动识别）：

`tar` `tar.gz` `tgz` `tar.bz2` `tbz2` `tar.xz` `txz` `tar.zst` `zip` `deb` `rpm` `appimage` `binary` `script`

### 1.2 命令行参数

| 参数 | 说明 |
|------|------|
| `-i, --package <path>` | 软件包路径（**非交互模式必需**） |
| `-d, --dir <path>` | 安装基目录，默认 `/opt`（实际装到 `<基目录>/<软件名>`） |
| `-n, --name <name>` | 软件名称（默认从包名推导） |
| `-t, --type <type>` | 包类型，默认 `auto` |
| `-e, --exec <path>` | 可执行文件绝对路径（跳过自动探测） |
| `-m, --mode <mode>` | `deb`/`rpm` 安装方式：`system`（包管理器，默认）/ `extract`（仅解包） |
| `--desktop` / `--no-desktop` | 创建 / 不创建 `.desktop` 快捷方式 |
| `--desktop-name <name>` | 显示名称 |
| `--icon <path>` | 图标路径 |
| `--category <cats>` | 分类，分号分隔，默认 `Utility` |
| `--terminal` | 程序需在终端中运行 |
| `--desktop-scope <s>` | `all` / `current` / `user` / `auto`（默认按环境判定） |
| `--desktop-user <ref>` | 指定用户（用户名 / UID / SID，隐含 `scope=user`） |
| `--desktop-fallback <f>` | 失败回退：`auto`（逐级降级，默认）/ `strict`（报错）/ `skip`（跳过） |
| `--desktop-sync-existing` | `all` 模式下同步到已存在用户的桌面目录 |
| `--desktop-public-dir <d>` | 覆盖公共桌面目录（默认 `/etc/skel/Desktop`） |
| `--service` / `--no-service` | 注册 / 不注册为服务 |
| `--service-manager <m>` | `systemd`/`upstart`/`sysv`/`openrc`/`runit`/`s6`/`supervisord`/`launchd`/`cron`/`rclocal`/`none`/`auto` |
| `--service-scope <s>` | `system`（系统级，需 root）/ `user`（用户级）/ `auto` |
| `--service-name <name>` | 服务名（默认软件名） |
| `--service-user <user>` | 运行用户（默认 `root`） |
| `--service-cmd <cmd>` | 启动命令（默认可执行文件路径） |
| `--service-workdir <dir>` | 工作目录（默认安装目录） |
| `--service-restart <p>` | 重启策略：`no`/`always`/`on-failure`/`on-abnormal`（默认 `on-failure`） |
| `--service-desc <text>` | 服务描述 |
| `--service-type <t>` | `simple`/`forking`/`oneshot`/`notify`（默认 `simple`） |
| `--service-after <a>` | 启动依赖目标（默认 `network.target`） |
| `--service-start <m>` | `now`（自启+启动）/ `boot`（仅自启）/ `none`（仅注册） |
| `--service-force` | 覆盖已存在且非本工具创建的服务定义 |
| `-y, --yes` | 非交互执行，确认取默认值 |
| `--explain` | 打印环境探测结果与模式判定依据后退出 |
| `--no-rollback` | 失败时不清理已生成内容（保留现场排查） |
| `-v, --verbose` | 输出调试信息 |
| `-h, --help` | 显示帮助 |

### 1.3 典型示例

```bash
# 交互式安装（逐项引导）
bash install_pkg.sh

# 静默安装归档包到 /opt
bash install_pkg.sh -i app.tar.gz -d /opt -y

# 安装并注册 systemd 服务，以 appuser 运行
bash install_pkg.sh -i app.tar.gz -y --desktop \
    --service --service-user appuser --service-cmd "/opt/app/bin/app"

# deb 绿色解包（不写包数据库）
bash install_pkg.sh -i pkg.deb -m extract -d /opt -y

# 仅查看本机环境探测与判定依据
bash install_pkg.sh --explain
```

### 1.4 注意事项

- 安装到 `/opt` 或注册系统级服务通常需要 root；脚本会先检测权限并提示。
- 桌面快捷方式与服务管理的「作用范围」按主机环境自动判定；跨用户/指定用户场景用 `--desktop-scope` / `--service-scope` 精确控制。
- 失败默认回滚；排查问题时可加 `--no-rollback` 保留现场。

---

## 2. set_mirror.sh — 软件源（镜像）管理工具

**版本**：1.0.0　**说明**：修改 `/etc` 下源配置需 root，脚本会自动 `sudo`/`pkexec` 提权；只读操作（`--list`/`--test`/`--status`/`--backup-list`）无需 root。

### 2.1 功能概述

自动识别发行版与版本，切换 / 追加 / 恢复软件源，并内置测速选优与 GPG 密钥修复。

支持的发行版 / 包管理器：

- Debian / Ubuntu / Deepin / UOS / Kali / Raspbian → `apt`
- CentOS / RHEL / Rocky / AlmaLinux / Fedora / openEuler → `yum`|`dnf`
- Alpine → `apk`
- openSUSE Leap / Tumbleweed / SLES → `zypper`

### 2.2 命令行参数

| 参数 | 说明 |
|------|------|
| `-l, --list` | 列出当前发行版可用的镜像源（无需 root） |
| `-s, --set <id\|URL>` | 切换到指定镜像（内置 ID 或完整 URL） |
| `-a, --add <id\|URL>` | 追加一个源，不替换现有配置 |
| `--auto` | 自动测速并选择最优源 |
| `-t, --test` | 仅测速并输出排序结果，不修改配置 |
| `--restore` | 恢复默认源（优先还原首次修改前的原始配置） |
| `--rollback [备份ID]` | 回滚到指定备份，默认最近一次 |
| `--backup-list` | 列出所有备份（无需 root） |
| `--status` | 显示当前生效的源配置（无需 root） |
| `--fix-gpg` | 仅修复 GPG 密钥问题 |
| `--update` | 仅更新软件包索引 |
| `-y, --yes` | 非交互执行 |
| `-n, --dry-run` | 预演模式，只输出将要执行的操作，不落盘 |
| `--no-update` | 修改后不执行索引更新 |
| `--no-gpg-fix` | 不自动修复 GPG 密钥问题 |
| `--distro <id>` | 手动指定发行版 ID |
| `--version <ver>` | 手动指定发行版版本号 |
| `--eol` | 强制按 EOL（归档源）处理 |
| `--no-epel` | RHEL 系不生成 EPEL 源 |
| `--with-src` | apt 同时生成 deb-src 源 |
| `--timeout <秒>` | 单次网络探测超时（默认见脚本 `NET_TIMEOUT`） |
| `--speed-url <URL>` | 指定测速抽样大文件 URL |
| `--parallel <n>` | 测速并发度（默认见脚本 `MAX_PARALLEL`） |
| `-k, --insecure` | curl 跳过 TLS 证书校验 |
| `-v, --verbose` | 输出调试信息 |
| `-h, --help` | 显示帮助 |

### 2.3 典型示例

```bash
# 交互式菜单
sudo bash set_mirror.sh

# 查看候选源（无需 root）
bash set_mirror.sh --list

# 自动测速并切换到最优源
sudo bash set_mirror.sh --auto

# 切换到阿里云
sudo bash set_mirror.sh --set aliyun

# 使用自定义 URL
sudo bash set_mirror.sh --set https://mirrors.tencent.com/debian

# 预演，不实际修改
sudo bash set_mirror.sh --set aliyun --dry-run

# 恢复默认源
sudo bash set_mirror.sh --restore

# 回滚上一次修改
sudo bash set_mirror.sh --rollback
```

### 2.4 注意事项

- **EOL 系统**：已停止维护的发行版版本会被强制走归档（archive）源，可显式 `--eol` 强制。
- **GPG 密钥**：源切换后出现 `NO_PUBKEY` / 签名无效时，脚本会自动下载导入并复检；可用 `--no-gpg-fix` 关闭。
- **幂等性**：重复执行同一 `--set` 不会重复污染配置；修改前均自动备份，回滚安全。
- **只读免 root**：`--list` / `--test` / `--status` / `--backup-list` 无需提权。

---

## 3. setup_firewall.sh — 防火墙配置工具

**设计模型**：入站白名单（`INPUT DROP`）+ 出站放行（`OUTPUT ACCEPT`，可切严格）。
**后端优先级**：`firewalld` > `ufw` > `nftables` > `iptables`（自动探测，多后端并存时按优先级选定）。

### 3.1 功能概述

交互式防火墙配置，统一规则接口在各后端下语义一致且幂等。特性：

- IPv4 + IPv6 双栈；Docker 兼容（不破坏 nat 表 / FORWARD 策略 / DOCKER* 链）。
- SSH 防暴力破解（各后端原生实现）。
- 常用服务预设模板（含 Oracle DB 1521）。
- 规则文件化 + 备份回滚 + 试运行超时自动回滚。

### 3.2 运行方式

脚本以交互菜单为主，无独立 CLI 子命令（提权后直接进入菜单）：

```bash
# 非 root 时自动提权
bash setup_firewall.sh

# 显式以 root 运行
sudo bash setup_firewall.sh
```

### 3.3 主菜单说明

| 编号 | 功能 |
|------|------|
| `1` | 显示当前防火墙状态 |
| `2` | 入站规则管理（放行 TCP / UDP 端口、服务预设、来源 IP/网段、列出、删除） |
| `3` | 出站规则管理（放行 / 丢弃、列出、删除） |
| `4` | 策略设置（INPUT / OUTPUT / FORWARD 默认 DROP↔ACCEPT 切换） |
| `5` | SSH 防暴力破解（开关、端口、时间窗、触发次数） |
| `6` | 切换 IPv6 启用状态 |
| `7` | 应用已配置规则（带确认） |
| `8` | 应用并持久化（`fw_apply` + `fw_persist`） |
| `9` | 备份管理（保存备份 / 回滚菜单） |
| `0` | 保存配置并退出 |

入站规则子菜单（`2`）常用操作：放行 TCP 端口、放行 UDP 端口、服务预设模板、按来源 IP/网段放行、列出当前规则、删除规则。

### 3.4 安全建议（务必先读）

- **远程操作前**：务必先放行当前 SSH 连接端口（脚本会尝试 `ensure_ssh_allowed`），避免把自己锁在门外。
- **先试后持久化**：用菜单 `7`（应用）验证连通性，确认无误后再 `8`（持久化）。
- **保留回滚点**：重大变更前用菜单 `9` 手动保存备份。

### 3.5 注意事项

- 必须 root 运行；直接 `sudo env` 绕过提权链会被二次校验拦截。
- 在 Docker 主机上，脚本不会触碰 nat 表与 FORWARD 默认策略，容器网络不受影响。
- 多防火墙后端并存时，以后端探测优先级为准；若行为与预期不符，先在主菜单 `1` 确认当前后端。

---

## 4. led_status.sh — 板载 LED 运行状态指示灯（守护进程）

**版本**：1.0.0　**退出码**：`0` 成功 / `1` 通用错误 / `2` 权限不足 / `3` 无可用 LED 设备 / `4` 参数错误 / `5` 已在运行

### 4.1 功能概述

为玩客云（OneCloud WS1608 / RK3328）等单板机编写的后台守护脚本，通过板载 LED 实时反映设备运行状态，无需人工干预：

| 设备状态 | LED 表现 | 含义 |
|----------|----------|------|
| 通电启动（未联网） | 第一种颜色（默认 **蓝**）常亮 | 已上电 |
| 成功联网 | 第二种颜色（默认 **绿**）常亮 | 网络已连接 |
| 有工作负载 | 绿灯常亮 + 第三种颜色（默认 **红**）闪烁 | 系统繁忙，闪烁频率随负载升高而加快 |

- **自动探测**：扫描 `/sys/class/leds`，按设备名匹配 `red` / `green` / `blue` 三个通道；仅单色 LED 时自动降级（常亮 / 闪烁区分状态）并给出告警。
- **状态自动切换**：网络探测优先 ping 默认网关，失败回退公网 DNS（223.5.5.5 / 8.8.8.8）；负载取 1 分钟 `loadavg` 除以 CPU 核数做归一化。
- **频率映射**：归一化负载 `0.10 → 2.0` 对应闪烁满周期 `1.2s → 0.15s`（负载越高闪得越快）；低于 `BUSY_THRESHOLD`(0.10) 视为空闲，仅绿灯常亮不闪。
- **守护与自启**：`start` 后台运行（写 PID 文件防重复启动）；`install` 注册 systemd 服务并开机自启；退出时自动清理 LED 状态，避免常亮卡死。

### 4.2 命令行参数（子命令）

| 子命令 | 说明 |
|--------|------|
| `run` | 前台运行（调试用，Ctrl-C 退出），日志输出到终端 |
| `start` | 后台守护进程，日志写入 `/var/log/led-status.log`，PID 写入 `/run/led-status.pid` |
| `stop` | 停止后台守护（发 SIGTERM，超时强杀） |
| `restart` | 重启（stop + start） |
| `status` | 查看运行状态（运行中 / 未运行） |
| `install` | 写入 `/etc/systemd/system/led-status.service` 并 `enable --now`（开机自启 + 立即启动）；改写前自动备份旧 unit |
| `uninstall` | 停用并删除 systemd 服务，同时停止进程 |
| `list-leds` | 列出本机 `/sys/class/leds` 下的可用 LED 设备及 `max_brightness`（无需 root） |
| `-h, --help` | 显示帮助 |

**环境变量（可选）**

| 变量 | 默认 | 说明 |
|------|------|------|
| `LED_BLUE` / `LED_GREEN` / `LED_RED` | 自动探测 | 分别指定 通电 / 联网 / 负载 三种状态对应的 `brightness` 节点路径 |
| `BUSY_THRESHOLD` | `0.10` | 归一化负载超过该值才开始闪烁 |
| `POLL_NET` | `3` | 未联网时重试检测的间隔（秒） |
| `POLL_IDLE` | `2` | 联网空闲时复查负载的间隔（秒） |

### 4.3 典型示例

```bash
# 查看本机 LED 设备(确认通道名/路径)
sudo bash led_status.sh list-leds

# 前台调试运行(观察状态切换与日志)
sudo bash led_status.sh run

# 部署为后台守护
sudo bash led_status.sh start
sudo bash led_status.sh status
sudo bash led_status.sh stop

# 安装为 systemd 服务, 开机自动指示状态
sudo bash led_status.sh install

# 单色 LED / 非标准命名时, 手动指定三个通道路径
sudo LED_BLUE=/sys/class/leds/status:blue:brightness \
     LED_GREEN=/sys/class/leds/status:green:brightness \
     LED_RED=/sys/class/leds/status:red:brightness \
     bash led_status.sh start
```

### 4.4 注意事项

- **必须 root**：LED 的 `brightness` 节点仅 root 可写；脚本会自动 `sudo` 提权，无 `sudo` 时报错退出码 `2`。
- **三色依赖 RGB 硬件**：标准玩客云板载多为单色 LED，此时三种状态会共用同一设备（通电/联网=常亮，负载=闪烁），无法显示真实三种颜色——脚本会明确告警。要看到真实三色，需设备具备 red/green/blue 三个独立 LED 通道。
- **bash >= 4.0**：脚本使用进程替换等特性，请在 bash 下运行（勿用 `sh`）。
- **避免与别的 LED 控制脚本冲突**：同一 LED 节点若被多个程序写，状态会互相覆盖；本脚本独占式控制，停止时清理为灭。
- **回归测试**：`tests/test_led_status.sh` 用 fake sysfs + stub 验证状态机与频率映射，无需真实硬件即可 `bash tests/test_led_status.sh` 跑通。
